import $ from '../util/domControll';
import icons from '../component/icons';

export default class MainHeader {
  constructor($root) {
    this.$root = $root;
    this.render();
  }

  render = () => {};
}
